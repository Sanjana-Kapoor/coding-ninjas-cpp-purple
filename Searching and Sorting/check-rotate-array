int arrayRotateCheck(int *input, int size)
{
    // int i,res =0;
    if(size <= 1)
    {
        return 0;
    }

    for(int i=0;i<size-1;i++)
    {  
        if(input[i]>input[i+1])
        {
            return i+1;
            
        }
    }
    return 0;
}
